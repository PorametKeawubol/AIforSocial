# Catalog data

`mercular_products.json` is an attributed, time-stamped snapshot generated by
`python scraper.py --refresh`. The LINE webhook reads this local file and never
crawls Mercular while a user is waiting for a reply.

Prices and availability are volatile. Refresh immediately before a classroom
demo, and do not treat the snapshot as an authoritative order quote.
